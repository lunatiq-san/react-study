// Don't use this example styles
const styles = {
  header: {
    minHeight: 80,
    backgroundColor: 'palevioletred',
  },
};

const AppBar = () => <header style={styles.header}></header>;

export default AppBar;
